'use client';
import { Play, Pause } from 'lucide-react';
export function TimeSlider({value,onChange,playing,onToggle,start,end}:{value:number;onChange:(v:number)=>void;playing:boolean;onToggle:()=>void;start?:string;end?:string}){const ready=!!start&&!!end&&start!==end;return <div className="timeline"><button className="icon-button" disabled={!ready} onClick={onToggle} aria-label={playing?'Pause timeline':'Play timeline'}>{playing?<Pause size={16}/>:<Play size={16}/>}</button><div className="timeline-track"><label htmlFor="time">Observation timeline <span>{ready?new Date(Date.parse(start)+(Date.parse(end)-Date.parse(start))*value).toLocaleDateString('en-GB',{timeZone:'UTC'}):'No observations loaded'}</span></label><input id="time" aria-label="Observation time" type="range" min="0" max="1" step="0.001" value={value} disabled={!ready} onChange={e=>onChange(+e.target.value)}/><div className="range-labels"><span>{start?.slice(0,10)||'—'}</span><span>{end?.slice(0,10)||'—'}</span></div></div><span className="tiny">UTC</span></div>}
